#include <stdio.h>
 
int main() {
    float dist, x1, y2, x2, y1;
    scanf("%f %f\n%f %f", &x1, &y1, &x2, &y2);
    dist = sqrt(pow(x2-x1,2) + pow(y2-y1,2));
    printf("%.4f\n", dist);
 
    return 0;
}
